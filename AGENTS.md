# Daydo 工作约定

- 使用中文交流和维护文档，标识符与 API 保持英文。
- 产品决策和验收事实统一维护在 docs/PLAN.md。
- macOS 14 起支持；SwiftUI 主应用、WidgetKit 小组件、同一可执行文件的 stdio MCP。
- Daydo 免登录使用；任务归属系统 iCloud 账户，未启用 iCloud 时可本机使用。不要恢复强制 Apple 登录或自建账号后端。
- 任务操作必须经过共享核心和数据空间校验。小组件及 MCP 不可建立旁路数据库。
- 业务行为先测试后实现。一次性测试使用 swift test 或 xcodebuild test。
- 单机测试不能证明双机同步；iCloud 错误不能显示为同步成功。
- 主应用、小组件、MCP 必须分别验证无 Daydo 登录凭据时可用，以及系统账户切换时隔离旧数据。
- 修改 SwiftUI 后检查真实窗口；不要仅以编译通过代替视觉验收。
- 保留本机数据和已有 iCloud 数据。禁止为调试重置已有 CloudKit 容器。
- 长命令继续等待原会话，高资源构建不并发，不使用宽泛进程终止命令。
